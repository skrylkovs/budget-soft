# Отчёт внедрения: /tehnologii/ruby/ — Ruby on Rails

Дата: 18.07.2026

## Итоговые зоны

- **Title:** Разработка на Ruby on Rails на заказ — веб-приложения, SaaS и MVP от 160 000 ₽ — BUDGET SOFT
- **H1:** Разработка на Ruby on Rails
- **Description:** Разработка на Ruby on Rails под ключ: MVP и SaaS, маркетплейсы, личные кабинеты, CRM и REST/GraphQL API. MVP за 4–8 недель, оплата поэтапно, 100% прав на код — у вас. 60+ проектов за 17 лет.
- **Лид:** ≤ 250 знаков (проверено)

## Карта покрытия ядра

Ядро собрано 18.07.2026, маска `разработка на ruby` — **3**.

| Запрос | Частота | Покрыт | Комментарий |
|---|---|---|---|
| разработка на ruby | 3 | да | |


## Блок → источник в генераторе

| Блок | Источник | Рендер |
|---|---|---|
| 1. Первый экран: H1, лид, интро-панель `.tech-intro` | поля `**H1:**`, лид-абзац, `**Facts:**` + `intro_image` в `TECH_META` | `render_tech_intro` |
| 2. Матрица решений «Что строим / Срок / Цена / Подробнее» | обычная MD-таблица | `.page-service-table` |
| 3. Вердикт-пары «подходит / не подходит» + сравнение | `{verdict-cards}` + MD-таблица | `tech_body_to_html` |
| 4. Карта экосистемы | `{eco-map}` | `tech_body_to_html` |
| 5. Две модели цены | `{price-models}` + MD-таблица | `tech_body_to_html` |
| 6. «Цифры» + Портфолио | — (в MD не переносится) | служебный: `render_page`, партиал `stats-cases.html` |
| 7. «Достался проект на X?» | `{rescue-cards}` | `RESCUE_ICONS` |
| 8. «X-разработчики в вашу команду» | `{staff-panel}` | `tech_body_to_html` |
| 9. «Как стартуем»: таймлайн 3 шагов | `{steps}` | `tech_body_to_html` |
| 10. FAQ (10 вопросов) | подсекция `### FAQ` | `render_tech_faq` + FAQPage в JSON-LD |
| 11. Финальный CTA | поле `**CTA:**` | служебный: `render_page` |
| Служебный блок «Отрасли» | `AI_DIRECTIONS_COPY["tehnologii-<slug>"]` | `render_ai_directions_block` |

## Программная верификация

- HTTP 200 (лендинг и хаб) — ок
- единственный H1 — ок
- canonical и Description — ок
- JSON-LD парсится, FAQPage в `@graph` — ок
- видимый FAQ ↔ FAQPage 1:1 (10 вопросов) — ок
- `page-spec-cards` / `compare--service` — 0 вхождений
- блок «Отрасли» (`ai-directions`) присутствует
- `section-lead` ≤ 250 — ок
- Title уникален в пределах сайта — ок

## ⚠️ Не подтверждены бизнесом — подтвердить до деплоя

- Цена веб-приложений (razrabotka-veb-prilozhenij) в инвентаре пустая — в матрице поставлено «от 400 000 ₽» по образцу Laravel-эталона; согласовать.
- Перенос цен со страниц услуг в Ruby-контекст (вся матрица «Что строим», Title «от 160 000 ₽», блок стоимости, FAQ 1) — связка цен именно с Ruby требует подтверждения бизнесом.
- Ставки аутстаффинга (общая от 1 400 ₽/час = QA, разработчик Middle+ от 1 900 ₽/час) взяты общесайтовые; Ruby-разработчики на рынке дефицитны и дороже (конкуренты 2 000–4 500 ₽/час) — возможно, ставка Ruby-инженера должна быть выше общей; согласовать.
- Версии Ruby 3.3+ / Rails 7+ — актуальность на момент публикации (блок «Экосистема»).
- FAQ 6 и staff-panel обещают «штат Ruby-разработчиков» и «подменный фонд / замена без остановки» — подтвердить фактическую кадровую обеспеченность по Ruby.
- Highload-аргумент (FAQ 5): Shopify/GitHub/Basecamp на Rails — публичный отраслевой факт; наши собственные кейсы на Ruby в портфолио не помечены — при необходимости добавить явный Ruby-кейс.
- Срок гарантийного периода в месяцах намеренно не называется (по правилу задачи) — при желании бизнеса опубликовать конкретный срок согласовать отдельно.

## Блокеры

1. ~~Семантическое ядро не собрано~~ — **СНЯТ 18.07.2026**: ядро собрано, см. выше.
2. **Hero-картинка `images/tech/ruby-hero.png` — плейсхолдер** (побайтовая копия
   `laravel-hero.png`): на странице нарисовано окно с Laravel-кодом. Генерирует пользователь.
3. **Индексация отложена** — переобход в Вебмастере и IndexNow только после деплоя.

## План контроля

- после сбора ядра — пересмотр зон Title/H1/матрицы и регенерация;
- после деплоя — переобход + IndexNow;
- сравнение метрик — ориентир 4–6 недель после переобхода.
