# Отчёт внедрения: /tehnologii/yii/ — Yii

Дата: 18.07.2026

## Итоговые зоны

- **Title:** Разработка на PHP-фреймворке Yii — сайты, порталы и веб-приложения от 150 000 ₽ — BUDGET SOFT
- **H1:** Разработка на Yii
- **Description:** Разработка на Yii под ключ: корпоративные порталы, CRM и ERP, магазины с 1С, личные кабинеты и API — от 150 000 ₽. MVP за 4–8 недель, оплата поэтапно, 100% прав на код — у вас. 60+ проектов за 17 лет.
- **Лид:** ≤ 250 знаков (проверено)

## Правки по ядру (18.07.2026)

Зоны пересмотрены после сбора Вордстата — см. блок ниже.

## Карта покрытия ядра

Ядро собрано 18.07.2026, маска `разработка на yii` — **0**.

Спроса по имени стека нет (Вордстат не показал строк) — страница
перенацелена на родительский интент, см. seo-core.


## Блок → источник в генераторе

| Блок | Источник | Рендер |
|---|---|---|
| 1. Первый экран: H1, лид, интро-панель `.tech-intro` | поля `**H1:**`, лид-абзац, `**Facts:**` + `intro_image` в `TECH_META` | `render_tech_intro` |
| 2. Матрица решений «Что строим / Срок / Цена / Подробнее» | обычная MD-таблица | `.page-service-table` |
| 3. Вердикт-пары «подходит / не подходит» + сравнение | `{verdict-cards}` + MD-таблица | `tech_body_to_html` |
| 4. Карта экосистемы | `{eco-map}` | `tech_body_to_html` |
| 5. Две модели цены | `{price-models}` + MD-таблица | `tech_body_to_html` |
| 6. «Цифры» + Портфолио | — (в MD не переносится) | служебный: `render_page`, партиал `stats-cases.html` |
| 7. «Достался проект на X?» | `{rescue-cards}` | `RESCUE_ICONS` |
| 8. «X-разработчики в вашу команду» | `{staff-panel}` | `tech_body_to_html` |
| 9. «Как стартуем»: таймлайн 3 шагов | `{steps}` | `tech_body_to_html` |
| 10. FAQ (10 вопросов) | подсекция `### FAQ` | `render_tech_faq` + FAQPage в JSON-LD |
| 11. Финальный CTA | поле `**CTA:**` | служебный: `render_page` |
| Служебный блок «Отрасли» | `AI_DIRECTIONS_COPY["tehnologii-<slug>"]` | `render_ai_directions_block` |

## Программная верификация

- HTTP 200 (лендинг и хаб) — ок
- единственный H1 — ок
- canonical и Description — ок
- JSON-LD парсится, FAQPage в `@graph` — ок
- видимый FAQ ↔ FAQPage 1:1 (10 вопросов) — ок
- `page-spec-cards` / `compare--service` — 0 вхождений
- блок «Отрасли» (`ai-directions`) присутствует
- `section-lead` ≤ 250 — ок
- Title уникален в пределах сайта — ок

## ⚠️ Не подтверждены бизнесом — подтвердить до деплоя

- Перенос цен услуг в Yii-контекст (матрица «Что строим», Title, абзац блока «Стоимость», FAQ 1–2) — цены взяты из инвентаря /uslugi/, связка с Yii [≈ согласовать]
- Веб-приложение, REST API: в инвентаре у razrabotka-veb-prilozhenij цена не указана — проставлено «от 400 000 ₽» (как в эталоне Laravel), требует подтверждения
- Ставки аутстаффинга в Yii-контексте (блоки «Стоимость»/«Команда», FAQ 1): общая от 1 400 ₽/час — QA, разработчик Middle+ от 1 900 ₽/час — существенно ниже рынка Yii-разработчиков (конкуренты: Middle 2500–2800, Senior 3300 ₽/час) [≈ согласовать]
- Версии стека: PHP 8+ и Yii 2.0 — подтвердить на момент публикации; обещание «автотесты и код-ревью входят в стоимость» — согласовать с бизнесом
- Срок гарантийного периода в месяцах в FAQ 10 намеренно не публикуется («фиксируем в договоре») — согласовать, публиковать ли конкретный срок
- Кейсы/портфолио и цифры добавляет каркас render_page, не MD; их стек не помечен как Yii — при желании добавить явный Yii-кейс в портфолио [≈ согласовать]

## Блокеры

1. ~~Семантическое ядро не собрано~~ — **СНЯТ 18.07.2026**: ядро собрано, см. выше.
2. **Hero-картинка `images/tech/yii-hero.png` — плейсхолдер** (побайтовая копия
   `laravel-hero.png`): на странице нарисовано окно с Laravel-кодом. Генерирует пользователь.
3. **Индексация отложена** — переобход в Вебмастере и IndexNow только после деплоя.

## План контроля

- после сбора ядра — пересмотр зон Title/H1/матрицы и регенерация;
- после деплоя — переобход + IndexNow;
- сравнение метрик — ориентир 4–6 недель после переобхода.
